# App
ENV=dev
LOG_LEVEL=INFO

# LLM
GOOGLE_API_KEY=your-google-api-key
OPENAI_API_KEY=sk-***replace-me***
OPENAI_MODEL=gpt-4o-mini

# Database (PostgreSQL with asyncpg driver)
# Use port 5433 if running Docker alongside local PostgreSQL
# Use port 5432 if only using Docker PostgreSQL
DATABASE_URL=postgresql+asyncpg://mindai_user:mindai_password@localhost:5433/mindai_adk
